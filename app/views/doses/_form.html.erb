<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.error_notification %>

  <%= f.input :description %>
  <%= f.association :ingredient, collection: Ingredient.all, prompt: "Ingredients" %>

  <%= f.submit "add a dose", class: "btn btn-primary" %>
<% end %>

